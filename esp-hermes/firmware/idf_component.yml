# ESP-Hermes firmware dependencies (ESP-IDF Component Manager).
# In v5+ `esp_websocket_client` moved out of the core IDF into the registry;
# the manager fetches it automatically at cmake time.
dependencies:
  esp_websocket_client:
    version: "^1.0.0"
  # json (cJSON) is in core IDF, no entry needed.
